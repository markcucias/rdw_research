[project]
name = "rdw"
version = "0.1.0"
description = "Self Driving Challenge perception algorithms"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
rdw-classical = "classical_prior.cli:app"